@{
    ViewData["Title"] = "Dashboard";

}
@model IEnumerable<Vehicle>

<!-- Dashboard Header -->
<div class="mb-4">
    <h1 class="text-primary">Fleet Dashboard</h1>
    <p class="text-muted">Overview of vehicles and status</p>
</div>
@{
    var lateststatuses = Model.SelectMany(v => v.VehicleStatuses)
        .GroupBy(s => s.VehicleId)
        .Select(g => g.OrderByDescending(s => s.StatusDate).FirstOrDefault())
		.ToList();

    var availableToday = lateststatuses
        .Count(x => x.Status == "Available" && x.StatusDate.Date == DateTime.Today);
    var unavailableToday = lateststatuses
        .Count(x => x.Status == "Unavailable" && x.StatusDate.Date == DateTime.Today);
    var damagedToday = lateststatuses
        .Count(x => x.Status == "Damaged" && x.StatusDate.Date == DateTime.Today);

}
<!-- Executive Summary Cards -->
<div class="row g-3 mb-4">
    <div class="col-md-3">
        <div class="card text-white bg-success shadow-sm h-100">
            <div class="card-body text-center">
                <h5 class="card-title">Available Vehicles</h5>
                <h2>@availableToday</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-danger shadow-sm h-100">
            <div class="card-body text-center">
                <h5 class="card-title">Unavailable Vehicles</h5>
                <h2>@unavailableToday</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-warning shadow-sm h-100">
            <div class="card-body text-center">
                <h5 class="card-title">Damaged Vehicles</h5>
                <h2>@damagedToday</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-primary shadow-sm h-100">
            <div class="card-body text-center">
                <h5 class="card-title">Total Vehicles</h5>
                <h2>@Model.Count()</h2>
            </div>
        </div>
    </div>
</div>

<!-- Vehicle Type Summary Table -->
<div class="mb-4">
    <h3 class="text-secondary mb-3">Vehicle Summary by Type</h3>
    @{
        var types = Model.Select(v => v.UnitType).Distinct().ToList();
    }
    <table class="table table-striped table-bordered align-middle text-center">
        <thead class="table-dark">
            <tr>
                <th>Type</th>
                <th>Owned</th>
                <th>Rented</th>
                <th>Lease</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var type in types)
            {
                <tr>
                    <td>@type</td>
                    <td>@Model.Count(v => v.OwnershipStatus == "Owned" && v.UnitType == type)</td>
                    <td>@Model.Count(v => v.OwnershipStatus == "Rented" && v.UnitType == type)</td>
                    <td>@Model.Count(v => v.OwnershipStatus == "Leased" && v.UnitType == type)</td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- Vehicle Management Table -->
<div class="mb-5">
    <h3 class="text-secondary mb-3">Vehicle Driver and availability</h3>
    <div class="table-responsive">
        <table class="table table-hover align-middle">
            <thead class="table-primary">
                <tr>
                    <th>Vehicle</th>
                    <th>Driver</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var v in Model)
                {
                    var latestStatus = v.VehicleStatuses
                        .OrderByDescending(s => s.StatusDate)
                        .FirstOrDefault();

                    <tr>
                        <td>@v.UnitModelSeries - @v.BrandMake</td>
                        <td>@(v.DriverInCharge?.FullName ?? "-")</td>
                        <td>
                            <span class="badge @(latestStatus?.Status == "Available" ? "bg-success" : latestStatus?.Status == "Unavailable" ? "bg-danger" : latestStatus?.Status == "Damaged" ? "bg-warning" : "text-dark")">
                                @(latestStatus?.Status ?? "Untagged")
                            </span>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<!-- Optional: Material Request Form -->
@* Uncomment if needed
<div class="mb-5">
    <h3 class="text-secondary mb-3">Material Request</h3>
    <div class="card shadow-sm p-4">
        <div class="mb-3">
            <label class="form-label">Material Name</label>
            <input type="text" class="form-control" placeholder="Enter material name">
        </div>
        <div class="mb-3">
            <label class="form-label">Quantity</label>
            <input type="number" class="form-control" placeholder="Enter quantity">
        </div>
        <button class="btn btn-primary w-100">Submit Request</button>
    </div>
</div>
*@
